<row r="${ROW}" customFormat="false" ht="${HEIGHT}" hidden="false" customHeight="1" outlineLevel="0" collapsed="false">
    <c r="${CELL}" s="${STYLE}" t="${TYPE}">
        <v>${VALUE}</v>
    </c>
</row>